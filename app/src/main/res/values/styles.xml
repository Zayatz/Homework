<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="switchOnStyle" parent="AppTheme">
        <item name="android:background">#f4e96e</item>
    </style>

    <style name="switchOffStyle" parent="AppTheme">
        <item name="android:background">#a0aced</item>
    </style>

    <style name="LayoutStyle" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/layoutHeigh</item>
        <item name="android:padding">@dimen/layoutPadding</item>
    </style>

    <style name="layoutItemStyle" parent="AppTheme">
        <item name="android:layout_margin">@dimen/layoutItemMargin</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="layoutItemStyle.layoutSmallItemStyle">
        <item name="android:layout_width">wrap_content</item>
    </style>
</resources>